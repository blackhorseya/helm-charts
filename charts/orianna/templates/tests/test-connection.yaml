apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orianna.fullname" . }}-test-connection"
  labels:
    {{- include "orianna.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orianna.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
